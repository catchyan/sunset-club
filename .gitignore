node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

.env
.env.*
!.env.example

.lanes/

*.log
npm-debug.log*
pnpm-debug.log*

.DS_Store
Thumbs.db
.vscode/
.idea/

coverage/
.nyc_output/
playwright-report/
test-results/

# Evidence packs keep their text and screenshots. Screen recordings are the one thing large
# enough to make cloning the repository slow, and re-running the command beats replaying one.
evidence/**/*.mp4
evidence/**/*.mov
evidence/**/*.webm

# Intermediates from asset generation. The inputs and the generator are committed; these are
# reproducible from them.
tools/asset-gen/.cache/
assets/**/*.blend1
