# T-001 · Upgrade the framework mirror to v3.0.2

- Owner: @A1
- Reviewer: @Q1
- Milestone: M0
- Release condition it advances: M0 — the repository governs itself before it holds a game.
  T-000 mounted v3.0.0; using it found two defects in the approval path and one in the docs
  gate, and this project is still pinned to the version that has all three.

## 1. Goal

`.studio-version` reads `v3.0.2`, the mirror matches the upstream tag exactly, and this
repository's own instructions no longer tell a reviewer to approve by commenting.

## 2. Inputs

- `sunset-studio` v3.0.2 — `CHANGELOG.md`, the v3.0.2 entry
- `.studio-version`, `docs/_studio/MANIFEST.json`
- `AGENTS.md` — says "comment" where the framework now says "review"
- `tools/studio-sync.mjs` — the mirror gate that checks the result

## 3. Lane

```
.studio-version
docs/_studio/**
AGENTS.md
evidence/T-001/**
```

## 4. Contracts

None frozen. v3.0.2 is a patch. One gate does change a verdict, and in the permissive
direction: the docs gate stops rejecting a card for naming the blocker report it has not
written. Nothing that passes today starts failing.

## 5. Definition of done

- [ ] `.studio-version` reads `v3.0.2`.
- [ ] `node tools/studio-sync.mjs --remote` passes — mirror matches the upstream file set,
      not merely the local manifest.
- [ ] `AGENTS.md` says a reviewer approves by submitting a review, and says a plain comment
      does not re-run the gates.
- [ ] `AGENTS.md` tells a reviewer what to do when the newest run is green and the merge is
      still blocked.

## 6. Acceptance command

```bash
node tools/studio-sync.mjs --remote
node docs/_studio/tools/verify-protection.mjs
```

## 7. Evidence

`evidence/T-001/` — command.txt, output.txt, diff-stat.txt, env.txt.

## 8. Timeout and escalation

- Expected: one session
- If blocked: escalate to @P0. A mirror that will not verify is a framework problem, not a
  game problem, and it belongs upstream.
- Three failed attempts: stop and write `board/blockers/T-001.md`. There is no fourth.
